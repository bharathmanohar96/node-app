apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-app-bharath.fullname" . }}
  labels:
    {{- include "node-app-bharath.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-app-bharath.selectorLabels" . | nindent 4 }}
