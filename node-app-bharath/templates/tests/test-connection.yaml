apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "node-app-bharath.fullname" . }}-test-connection"
  labels:
    {{- include "node-app-bharath.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "node-app-bharath.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
